fastapi>=0.115.0
uvicorn[standard]>=0.34.0
httpx>=0.28.0
openai>=1.75.0
stripe>=12.0.0
python-dotenv>=1.0.0
uagents>=0.22.0
uagents-core>=0.4.0

# Local browser backend (Steel + browser-use). Optional — only needed when
# BROWSER_BACKEND=steel. Heavyweight: pulls in playwright. Comment these out if
# you only want the cloud BrowserUse SDK from the Next.js side.
browser-use>=0.1.40
langchain-openai>=0.2.0
